<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CineCritique" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

        <!-- Custom Rating Button Style -->
        <style name="RatingButtonStyle" parent="Widget.Material3.Button.Icon">
            <item name="backgroundTint">@color/fui_transparent</item>
            <item name="iconTint">@color/white</item>
            <item name="iconGravity">textTop</item>
            <item name="iconPadding">0dp</item>
            <item name="iconSize">20dp</item>
            <item name="android:padding">0dp</item>
            <item name="android:insetLeft">0dp</item>
            <!--<item name="android:insetTop">0dp</item>-->
            <item name="android:insetBottom">0dp</item>
            <item name="android:insetRight">0dp</item>
        </style>

    <style name="Theme.CineCritique" parent="Base.Theme.CineCritique" />
</resources>